import {Button, styled} from '@mui/material';

export const AppButton = styled(Button)(() => ({
    borderRadius: '4px',
    backgroundColor: '#1900D5',
    padding: '10px 20px',
    maxWidth: '300px',
}));